# Add a vertical scrollbar to the right frame
scrollbar = ttk.Scrollbar(left_frame, orient='vertical', command=left_frame.yview)
scrollbar.pack(side='left', fill='y')

# Configure the text widget to use the scrollbar
left_frame.config(yscrollcommand=scrollbar.set)
